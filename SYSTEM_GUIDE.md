# 📚 دليل نظام إدارة وتحليل الوثائق الذكي

## 🎯 نظرة عامة

نظام متكامل لإدارة وتحليل الوثائق باستخدام الذكاء الاصطناعي و OCR، مصمم خصيصاً لمركز الاستشارات والتنمية.

---

## ✨ الميزات الرئيسية

### 1️⃣ **رفع ومعالجة ذكية للوثائق**
- ✅ دعم أنواع متعددة: PDF, Word (.doc, .docx), Excel (.xls, .xlsx), صور
- ✅ استخراج نص تلقائي باستخدام OCR (Tesseract)
- ✅ تصنيف تلقائي (شهادة، تقرير، كتاب رسمي، نموذج)
- ✅ اقتراح عنوان ذكي من محتوى الوثيقة
- ✅ استخراج البيانات الوصفية (تاريخ، صادر/وارد)
- ✅ تحويل تلقائي إلى PDF
- ✅ حفظ منظم حسب السنة/الشهر/رقم الوثيقة

### 2️⃣ **نظام التخزين المنظم**
المسار الرئيسي: `D:\مركز الاستشارات والتنمية`

```
D:\مركز الاستشارات والتنمية\
├── originals/          # الملفات الأصلية
│   └── 2025/
│       └── 11/
│           └── 20251113-143025/
│               ├── original.docx
│               └── manifest.json
├── pdf/                # نسخ PDF
│   └── 2025/11/
│       └── 20251113-143025.pdf
├── images/             # صور الصفحات
│   └── 2025/11/
│       └── 20251113-143025/
│           ├── page_001.png
│           └── page_002.png
├── ocr_text/           # النصوص المستخرجة
│   └── 2025/11/
│       └── 20251113-143025.txt
├── thumbnails/         # معاينات صغيرة
│   └── 2025/11/
│       └── 20251113-143025.png
├── backups/            # نسخ احتياطية
├── temp/               # ملفات مؤقتة
└── logs/               # سجلات النظام
```

### 3️⃣ **إدارة المستخدمين المتقدمة**
- ✅ إضافة/تعديل/تعطيل/حذف المستخدمين
- ✅ صلاحيات مرنة لكل مستخدم
- ✅ حالة الاتصال الحقيقية (Online/Offline)
- ✅ تغيير كلمة المرور الإجباري

### 4️⃣ **البحث المتقدم**
- ✅ بحث في العنوان أو المحتوى أو الاثنين
- ✅ فلترة حسب نوع الوثيقة (شهادة، تقرير، إلخ)
- ✅ فلترة حسب الاتجاه (صادر/وارد)
- ✅ فلترة حسب التاريخ
- ✅ عرض مقتطفات من النص مع تمييز كلمات البحث

### 5️⃣ **عرض وتحليل الوثائق**
- ✅ عرض جميع البيانات الوصفية
- ✅ عرض النص المستخرج كاملاً
- ✅ معاينة الوثيقة
- ✅ دقة OCR مع مؤشر ملون
- ✅ إمكانية الطباعة

### 6️⃣ **التعديل الآمن**
- ✅ تعديل العنوان والبيانات الوصفية
- ✅ **منع تعديل المحتوى الأصلي** (حماية)
- ✅ تسجيل جميع التعديلات في سجل النشاط

### 7️⃣ **سجل النشاط الشامل**
- ✅ تسجيل جميع العمليات
- ✅ تفاصيل مفهومة (بدون JSON)
- ✅ فلترة حسب المستخدم والإجراء والتاريخ
- ✅ تحديث تلقائي

---

## 🤖 التصنيف التلقائي الذكي

النظام يحلل محتوى الوثيقة ويصنفها تلقائياً:

### أنواع الوثائق:
1. **شهادة**: يحتوي على كلمات (شهادة، يشهد، certificate)
2. **تقرير**: يحتوي على (تقرير، report، تقييم)
3. **كتاب رسمي**: يحتوي على (كتاب، إلى، السيد المحترم)
4. **نموذج**: يحتوي على (نموذج، طلب، استمارة)
5. **أخرى**: ما لا ينطبق عليه التصنيفات السابقة

### تحديد الاتجاه:
- **صادر**: يحتوي على (صادر، من:، المرسل)
- **وارد**: يحتوي على (وارد، إلى:، المستلم، المحترم)

### استخراج التاريخ:
- يبحث عن تواريخ بصيغة: YYYY-MM-DD أو DD/MM/YYYY
- يستخرج أول تاريخ يجده في الوثيقة

### اقتراح العنوان:
- يأخذ أول 10 كلمات من الوثيقة (عادة تكون العنوان)
- ينظف النص من الرموز الخاصة
- يحدد الطول المناسب (100 حرف كحد أقصى)

---

## 🔐 نظام الصلاحيات

### مدير النظام:
- ✅ كل الصلاحيات
- ✅ إدارة المستخدمين
- ✅ عرض سجل النشاط
- ✅ عرض جميع الوثائق
- ✅ تحليل جميع الوثائق

### موظف:
- ✅ رفع وثائق جديدة
- ✅ عرض وثائقه فقط (أو حسب الصلاحيات)
- ✅ تعديل وثائقه
- ✅ البحث في وثائقه
- ⚙️ صلاحيات قابلة للتخصيص

---

## 🚀 كيفية الاستخدام

### رفع وثيقة جديدة:

1. **اذهب إلى "رفع وثيقة"**
2. **اختر المصدر**:
   - 📁 ملف من الجهاز
   - 🖨️ سكانر (قريباً)
3. **اختر الملف**: اسحب الملف أو اضغط "اختيار ملف"
4. **العنوان (اختياري)**: اترك فارغاً للحصول على عنوان مقترح تلقائياً
5. **اضغط "🚀 رفع وتحليل الوثيقة"**

### ما يحدث تلقائياً:
- ✅ استخراج النص من الملف
- ✅ تصنيف نوع الوثيقة
- ✅ تحديد صادر/وارد
- ✅ استخراج التاريخ
- ✅ اقتراح عنوان
- ✅ تحويل إلى PDF
- ✅ حفظ منظم في المسار المحدد

### البحث عن وثيقة:

1. **اذهب إلى "بحث متقدم"**
2. **أدخل كلمة البحث**
3. **اختر نطاق البحث**: الكل، العنوان فقط، المحتوى فقط
4. **استخدم الفلاتر**:
   - نوع الوثيقة
   - الاتجاه (صادر/وارد)
5. **اضغط "🔍 بحث"**

### عرض وثيقة:

1. **اذهب إلى "عرض الوثائق"**
2. **اضغط "عرض" على أي وثيقة**
3. **ستظهر التفاصيل**:
   - البيانات الوصفية
   - النص المستخرج
   - دقة OCR
   - التصنيف والاتجاه

### تعديل وثيقة:

1. **افتح الوثيقة**
2. **اضغط "✏️ تعديل البيانات الوصفية"**
3. **يمكنك تعديل**:
   - ✅ العنوان
   - ✅ التصنيف
   - ✅ الاتجاه (صادر/وارد)
   - ✅ التاريخ
4. **لا يمكنك تعديل**:
   - ❌ المحتوى النصي (محمي)
   - ❌ الملف الأصلي (محمي)

---

## 🛠️ التقنيات المستخدمة

### Backend:
- **FastAPI**: إطار عمل API سريع
- **SQLAlchemy**: ORM لقاعدة البيانات
- **PostgreSQL**: قاعدة بيانات قوية
- **Tesseract OCR**: استخراج النص من الصور
- **PyMuPDF**: معالجة ملفات PDF
- **python-docx**: معالجة ملفات Word
- **openpyxl**: معالجة ملفات Excel
- **Redis**: حالة الاتصال والـ cache
- **Celery**: معالجة الخلفية

### Frontend:
- **React + TypeScript**: واجهة تفاعلية
- **Tailwind CSS**: تصميم عصري
- **Axios**: طلبات HTTP
- **React Router**: التنقل

---

## 📊 هيكل قاعدة البيانات

### جدول `documents`:
- رقم الوثيقة (فريد)
- العنوان (يدوي)
- العنوان المقترح (تلقائي)
- النص المستخرج
- التصنيف (تلقائي)
- الاتجاه (صادر/وارد - تلقائي)
- التاريخ الأصلي
- نوع المصدر (ملف/سكانر)
- المسارات (original, pdf, ocr_text, images)
- دقة OCR
- الحالة (completed, processing, failed)

### جدول `activity_log`:
- المستخدم
- الإجراء
- التفاصيل
- عنوان IP
- التاريخ والوقت

### جدول `users`:
- جميع بيانات المستخدم
- الصلاحيات
- الدور

---

## 🎨 دليل الألوان

- **Cyan (#00BCD4)**: الأزرار الرئيسية، العناوين، الروابط النشطة
- **Green**: نجاح، نشط، متصل، دقة عالية
- **Yellow/Orange**: تحذير، قيد المعالجة، دقة متوسطة
- **Red**: خطأ، حذف، معطل، دقة منخفضة
- **Gray**: خلفيات، نصوص ثانوية، غير متصل

---

## 🔑 بيانات الدخول الافتراضية

### مدير النظام:
- اسم المستخدم: `admin`
- كلمة المرور: `admin123`

⚠️ **يُنصح بتغيير كلمة المرور فوراً!**

---

## 🚀 التشغيل

### المرة الأولى:

```powershell
# 1. بناء وتشغيل Docker
cd docker
docker compose build
docker compose up -d

# 2. الانتظار 10 ثوان لإنشاء قاعدة البيانات

# 3. تشغيل Frontend
cd ../frontend
npm install
npm run dev

# 4. فتح المتصفح
# http://localhost:5173
```

### التشغيل العادي:

```powershell
# Backend
cd docker
docker compose up -d

# Frontend
cd ../frontend
npm run dev
```

---

## 📝 ملاحظات مهمة

### حول OCR:
- دقة OCR تعتمد على جودة الصورة/الملف
- الملفات الواضحة (Word, PDF نصي) → دقة 100%
- الصور الممسوحة ضوئياً → دقة 60-95%
- يمكن تحسين الدقة بتحسين جودة المسح

### حول التخزين:
- جميع الملفات تُحفظ في مسار منظم
- يتم إنشاء `manifest.json` لكل وثيقة
- النسخ الاحتياطية تبقى حتى بعد الحذف من قاعدة البيانات

### حول الأمان:
- المحتوى الأصلي **محمي** من التعديل
- فقط البيانات الوصفية قابلة للتعديل
- جميع العمليات مسجلة في سجل النشاط

---

## 🎉 الميزات المستقبلية (قريباً)

- [ ] دعم السكانر المباشر
- [ ] معاينة PDF داخل الواجهة
- [ ] تحميل مجموعة ملفات دفعة واحدة
- [ ] تصدير تقارير
- [ ] نسخ احتياطي تلقائي
- [ ] إشعارات فورية
- [ ] تكامل مع أنظمة خارجية

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. افتح Console (F12) وأرسل لقطة شاشة
2. افحص logs: `docker logs docker-api-1 --tail 50`
3. تحقق من المسار: `D:\مركز الاستشارات والتنمية`

---

**🎉 النظام جاهز للاستخدام! استمتع بالإدارة الذكية للوثائق!** 🚀



